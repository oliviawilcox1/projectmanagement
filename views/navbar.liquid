{% block content %}
<nav class="navbar navbar-expand-md navbar-dark bg-light">
    <div class="container-fluid">
        <a href="/" class="navbar-brand text-dark"> </a>
        {% if loggedIn %}
        <a href="" class="nav-link text-dark"> Hello {{ username | capitalize }}! </a>
        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if loggedIn %}
            <a href="/tasks/new" class="nav-link text-dark">Create New Task</a>
            <a href="/tasks" class="nav-link text-dark">All Tasks</a>
            <a href="/memos" class="nav-link text-dark">All Memos</a>
            <!-- <a href="/jobs/filter" class="nav-link text-dark">Filter</a> -->
            <a href="/auth/logout" class="nav-link text-dark">Log Out</a>
            {% else %}
                <a href="/auth/signup" class="nav-link text-dark">Sign Up</a>
                <a href="/auth/login" class="nav-link text-dark">Sign In</a>
            {% endif %}
        </div>
    </div>
</nav>
{% endblock %}